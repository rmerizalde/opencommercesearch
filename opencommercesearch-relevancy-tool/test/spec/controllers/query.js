'use strict';

describe('Controller: QueryCtrl', function() {
    beforeEach(module('relevancyApp'));

    var QueryCtrl,
        scope;

    /*
    beforeEach(inject(function($controller, $rootScope) {
        scope = $rootScope.$new();
        QueryCtrl = $controller('QueryCtrl as queryCtrl', {
            $scope: scope
        });
    }));
    */
});